# 缩放点积注意力

```python
import torch
import math

def scaled_dot_product_attention(query: torch.Tensor, key: torch.Tensor, value: torch.Tensor, mask: torch.Tensor = None)->tuple[torch.Tensor, torch.Tensor]:
    """
    计算缩放点积注意力。

    参数：
        query: 查询张量；形状为 (batch_size, num_heads, seq_len_q, d_k)
               或 (batch_size, seq_len_q, d_k) (如果为单头)。
        key: 键张量；形状为 (batch_size, num_heads, seq_len_k, d_k)
             或 (batch_size, seq_len_k, d_k) (如果为单头)。
        value: 值张量；形状为 (batch_size, num_heads, seq_len_v, d_v)
               或 (batch_size, seq_len_v, d_v) (如果为单头)。
               注意：seq_len_k 和 seq_len_v 必须相同。
        mask: 可选的掩码张量；形状应可广播到
              (batch_size, num_heads, seq_len_q, seq_len_k)。
              `True` 或 `1` 的位置将被掩盖（设为 -inf）。

    返回：
        包含以下内容的元组：
        - output：注意力输出张量；
                  形状为 (batch_size, num_heads, seq_len_q, d_v)
                  或 (batch_size, seq_len_q, d_v) (如果为单头)。
        - attention_weights：注意力权重张量；
                             形状为 (batch_size, num_heads, seq_len_q, seq_len_k)
                             或 (batch_size, seq_len_q, seq_len_k) (如果为单头)。
    """

    # 获取键向量的最后一维大小（d_k）
    d_k = query.size(-1)
    # 计算缩放点积：(Q @ K^T) / sqrt(d_k)
    scores = torch.matmul(query, key.transpose(-2, -1)) / math.sqrt(d_k)

    # 如果提供了掩码，则将掩码位置设为负无穷
    if mask is not None:
        scores = scores.masked_fill(mask, float('-inf'))

    # 对分数应用softmax以获得注意力权重，dim=-1表示最后一个维度，即seq_len_k，每一行
    attention_weights = torch.nn.functional.softmax(scores, dim=-1)

    # 处理NaN值，将其替换为0.0
    if torch.isnan(attention_weights).any():
        attention_weights = torch.nan_to_num(attention_weights, nan=0.0)

    # 使用注意力权重加权求和值向量
    output = torch.matmul(attention_weights, value)
    # 返回输出和注意力权重
    return output, attention_weights

# 示例用法
batch_size = 2
seq_len_q = 5
seq_len_k = 7
d_k = 64
d_v = 128

query_tensor = torch.randn(batch_size, seq_len_q, d_k)
key_tensor = torch.randn(batch_size, seq_len_k, d_k)
value_tensor = torch.randn(batch_size, seq_len_k, d_v)

# 掩码示例
padding_mask = torch.zeros(batch_size, 1,seq_len_k, dtype=torch.bool)
padding_mask[:, :, -2:] = True

# 调用缩放点积注意力函数
output, attention_weights = scaled_dot_product_attention(query_tensor, key_tensor, value_tensor, padding_mask)

print("Output shape:", output.shape)
print("attention weights shape:", attention_weights.shape)


print(attention_weights)

```

Output:

```
Output shape: torch.Size([2, 5, 128])
attention weights shape: torch.Size([2, 5, 7])
tensor([[[0.2407, 0.3483, 0.1614, 0.0996, 0.1500, 0.0000, 0.0000],
         [0.1174, 0.4085, 0.1518, 0.0798, 0.2424, 0.0000, 0.0000],
         [0.1891, 0.0479, 0.1398, 0.5030, 0.1201, 0.0000, 0.0000],
         [0.0678, 0.1411, 0.1582, 0.4408, 0.1921, 0.0000, 0.0000],
         [0.2481, 0.1345, 0.2268, 0.1348, 0.2557, 0.0000, 0.0000]],

        [[0.2220, 0.1610, 0.1945, 0.3165, 0.1060, 0.0000, 0.0000],
         [0.2681, 0.1159, 0.3272, 0.2503, 0.0385, 0.0000, 0.0000],
         [0.0844, 0.1331, 0.2621, 0.3907, 0.1297, 0.0000, 0.0000],
         [0.1515, 0.4817, 0.1745, 0.1469, 0.0454, 0.0000, 0.0000],
         [0.2765, 0.1614, 0.1006, 0.0459, 0.4156, 0.0000, 0.0000]]])
```

# 多头注意力

```python
import torch
import math

def scaled_dot_product_attention(query: torch.Tensor, key: torch.Tensor, value: torch.Tensor, mask: torch.Tensor = None)->tuple[torch.Tensor, torch.Tensor]:
    """
    计算缩放点积注意力。

    参数：
        query: 查询张量；形状为 (batch_size, num_heads, seq_len_q, d_k)
               或 (batch_size, seq_len_q, d_k) (如果为单头)。
        key: 键张量；形状为 (batch_size, num_heads, seq_len_k, d_k)
             或 (batch_size, seq_len_k, d_k) (如果为单头)。
        value: 值张量；形状为 (batch_size, num_heads, seq_len_v, d_v)
               或 (batch_size, seq_len_v, d_v) (如果为单头)。
               注意：seq_len_k 和 seq_len_v 必须相同。
        mask: 可选的掩码张量；形状应可广播到
              (batch_size, num_heads, seq_len_q, seq_len_k)。
              `True` 或 `1` 的位置将被掩盖（设为 -inf）。

    返回：
        包含以下内容的元组：
        - output：注意力输出张量；
                  形状为 (batch_size, num_heads, seq_len_q, d_v)
                  或 (batch_size, seq_len_q, d_v) (如果为单头)。
        - attention_weights：注意力权重张量；
                             形状为 (batch_size, num_heads, seq_len_q, seq_len_k)
                             或 (batch_size, seq_len_q, seq_len_k) (如果为单头)。
    """

    # 获取键向量的最后一维大小（d_k）
    d_k = query.size(-1)
    # 计算缩放点积：(Q @ K^T) / sqrt(d_k)
    scores = torch.matmul(query, key.transpose(-2, -1)) / math.sqrt(d_k)

    # 如果提供了掩码，则将掩码位置设为负无穷
    if mask is not None:
        scores = scores.masked_fill(mask, float('-inf'))

    # 对分数应用softmax以获得注意力权重，dim=-1表示最后一个维度，即seq_len_k，每一行
    attention_weights = torch.nn.functional.softmax(scores, dim=-1)

    # 处理NaN值，将其替换为0.0
    if torch.isnan(attention_weights).any():
        attention_weights = torch.nan_to_num(attention_weights, nan=0.0)

    # 使用注意力权重加权求和值向量
    output = torch.matmul(attention_weights, value)
    # 返回输出和注意力权重
    return output, attention_weights


class MultiHeadAttention(torch.nn.Module):
    # 定义多头注意力类，继承自torch.nn.Module
    def __init__(self, d_model: int, num_heads: int, dropout: float = 0.1):
        """
        初始化多头注意力模块。

        参数：
            d_model: 模型的隐藏维度。
            num_heads: 注意力头的数量。
            dropout: Dropout的概率。
        """

        # 调用父类的初始化方法
        super().__init__()

        # 断言d_model能被num_heads整除
        assert d_model % num_heads == 0, "d_model must be divisible by num_heads"

        # 存储模型隐藏维度
        self.d_model = d_model
        # 存储注意力头的数量
        self.num_heads = num_heads

        # 计算每个头的维度
        self.d_k = d_model // num_heads

        # 创建查询的线性变换层
        self.W_q = torch.nn.Linear(d_model, d_model)
        # 创建键的线性变换层
        self.W_k = torch.nn.Linear(d_model, d_model)
        # 创建值的线性变换层
        self.W_v = torch.nn.Linear(d_model, d_model)

        # 创建输出的线性变换层
        self.W_o = torch.nn.Linear(d_model, d_model)
        # 创建Dropout层
        self.dropout = torch.nn.Dropout(dropout)  # Dropout层（可选）

    def split_heads(self, x: torch.Tensor) -> torch.Tensor:
        """
        将输入张量拆分为多头。

        参数：
            x: 输入张量，形状为 (batch_size, seq_len, d_model)

        返回：
            拆分后的张量，形状为 (batch_size, num_heads, seq_len, d_k)
        """
        # 获取批量大小和序列长度
        batch_size, seq_len, _ = x.size()
        # 重新调整张量形状为 (batch_size, seq_len, num_heads, d_k)
        x = x.view(batch_size, seq_len, self.num_heads, self.d_k)
        # 转置第1和2维，得到 (batch_size, num_heads, seq_len, d_k)
        return x.transpose(1, 2)  # (batch_size, num_heads, seq_len, d_k)

    def combine_heads(self, x: torch.Tensor) -> torch.Tensor:
        """
        将多头张量合并为单头。

        参数：
            x: 多头张量，形状为 (batch_size, num_heads, seq_len, d_k)

        返回：
            合并后的张量，形状为 (batch_size, seq_len, d_model)
        """
        # 获取多头张量的维度信息
        batch_size, num_heads, seq_len, d_k = x.size()
        # 转置第1和2维得到 (batch_size, seq_len, num_heads, d_k)，并保证内存连续
        x = x.transpose(1, 2).contiguous()  # (batch_size, seq_len, num_heads, d_k)
        # 重新调整张量形状为 (batch_size, seq_len, d_model)
        return x.view(batch_size, seq_len, num_heads * d_k)  # (batch_size, seq_len, d_model)

    def forward(self, query: torch.Tensor, key: torch.Tensor, value: torch.Tensor, mask: torch.Tensor = None) -> torch.Tensor:
        """
        多头注意力的前向传播。

        参数：
            query: 查询张量，形状为 (batch_size, seq_len_q, d_model)
            key: 键张量，形状为 (batch_size, seq_len_k, d_model)
            value: 值张量，形状为 (batch_size, seq_len_k, d_model)
            mask: 可选的掩码张量，形状为 (batch_size, 1, seq_len_k) 或 (batch_size, seq_len_q, seq_len_k)

        返回：
            输出张量，形状为 (batch_size, seq_len_q, d_model)
            注意力权重，形状为 (batch_size, num_heads, seq_len_q, seq_len_k)
        """
        # 对查询进行线性变换
        Q = self.W_q(query)
        # 对键进行线性变换
        K = self.W_k(key)
        # 对值进行线性变换
        V = self.W_v(value)

        # 将查询拆分为多个头
        Q = self.split_heads(Q)
        # 将键拆分为多个头
        K = self.split_heads(K)
        # 将值拆分为多个头
        V = self.split_heads(V)

        # 计算缩放点积注意力并获得输出和权重
        output, attention_weights = scaled_dot_product_attention(Q, K, V, mask)

        # 将多个头的输出合并为单头
        output = self.combine_heads(output)

        # 对合并后的输出进行线性变换
        output = self.W_o(output)
        # 应用Dropout正则化
        output = self.dropout(output)

        # 返回最终输出和注意力权重
        return output, attention_weights

batch_size = 2
seq_len = 10
d_model = 512
num_heads = 8

x = torch.rand(batch_size, seq_len, d_model)

mha = MultiHeadAttention(d_model, num_heads)
output, attention_weights = mha(x, x, x)

print("input shape:", x.shape)
print("output shape:", output.shape)
print("attention weights shape:", attention_weights.shape)
```

Output:

```
input shape: torch.Size([2, 10, 512])
output shape: torch.Size([2, 10, 512])
attention weights shape: torch.Size([2, 8, 10, 10])
```

疑问：Q = self.W_q(query) 进行切分哪里没搞懂，这样不是每个头都是用的同一个线性变化么？没有体现一个头一个query 权重w么？

解答：

原论文（Attention is All You Need）里写的就是这种形式：

$$
\text{head}_i = \text{Attention}(QW_i^Q, KW_i^K, VW_i^V)
$$
其中每个 \(W_i^Q\) 的形状是 \(d_{\text{model}} \times d_k\)。如果把所有 head 的投影矩阵水平拼起来：



$$
W^Q = \begin{bmatrix} W_1^Q & W_2^Q & \cdots & W_h^Q \end{bmatrix}
\quad (d_{\text{model}} \times d_{\text{model}})
$$
然后一次矩阵乘法：

```python
Q = X @ W^Q          # 得到 (batch, seq, d_model)
```

其实与公式多头计算是一致的，这里这样一次大矩阵乘法是方便加快计算。

# 位置感知前馈网络(Position-wise Feed Forward Network)

### 有用的知识

通用近似定理说明：只要一个前馈神经网络拥有足够多的神经元和一个合适的非线性激活函数，它就能够以任意精度逼近任何连续函数。

注意力层负责“看别人”（token 与 token 之间的信息交流），前馈网络负责“想自己”（对每个 token 的信息进行深度加工）。

- **Self-Attention**：关注“横向”——Token 与 Token 之间的关系（你在哪里，和谁相关）
- **Position-wise FFN**：关注“纵向”——单个 Token 自身的深层特征变换与加工（这个词本身蕴含着怎样的复杂语义）。

```python
import torch
import math

class PositionwiseFeedForward(torch.nn.Module):
    def __init__(self, d_model: int, d_ff: int, dropout: float = 0.1):
        # 调用父类初始化方法
        super(PositionwiseFeedForward, self).__init__()
        # 创建第一个线性层，将输入从d_model维映射到d_ff维
        self.linear1 = torch.nn.Linear(d_model, d_ff)
        # 创建ReLU激活函数
        self.activation = torch.nn.ReLU()
        # 创建第二个线性层，将输入从d_ff维映射回d_model维
        self.linear2 = torch.nn.Linear(d_ff, d_model)
        # 创建Dropout层以进行正则化
        self.dropout = torch.nn.Dropout(dropout)

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        # 通过第一个线性层处理输入
        x = self.linear1(x)
        # 应用ReLU激活函数
        x = self.activation(x)
        # 通过第二个线性层处理输出
        x = self.linear2(x)
        # 应用Dropout正则化
        x = self.dropout(x)
        # 返回处理后的输出
        return x
```

# 构建编码器层和解码器层

### 残差连接

```
x = x + attn_output
y=x+F(x)
```

核心思想：不要让网络只学习一个完整的映射，而是让网络学习“输入和输出之间的差异（残差）。可以防止反向传播中的梯度消失的问题。

### 编码器层结构图

```
                 Encoder Layer

                    输入 X
                      |
                      |
              +---------------+
              | Multi-Head    |
              | Self Attention |
              +---------------+
                      |
                      |
                 残差连接
                      |
                 LayerNorm
                      |
                      |
              +---------------+
              | Feed Forward  |
              | Network (FFN) |
              +---------------+
                      |
                 残差连接
                      |
                 LayerNorm
                      |
                      |
                   输出
```

### 解码器层结构图

```
                    Decoder Layer


                      输入
                       |
                       |
          +--------------------------+
          | Masked Multi-Head        |
          | Self Attention            |
          +--------------------------+
                       |
                  Add & Norm
                       |
                       |
          +--------------------------+
          | Encoder-Decoder          |
          | Attention                |
          +--------------------------+
                       |
                  Add & Norm
                       |
                       |
          +--------------------------+
          | Feed Forward Network     |
          +--------------------------+
                       |
                  Add & Norm
                       |
                       |
                     输出
```

### 代码

```python
import torch
import math

def scaled_dot_product_attention(query: torch.Tensor, key: torch.Tensor, value: torch.Tensor, mask: torch.Tensor = None)->tuple[torch.Tensor, torch.Tensor]:
    """
    计算缩放点积注意力。

    参数：
        query: 查询张量；形状为 (batch_size, num_heads, seq_len_q, d_k)
               或 (batch_size, seq_len_q, d_k) (如果为单头)。
        key: 键张量；形状为 (batch_size, num_heads, seq_len_k, d_k)
             或 (batch_size, seq_len_k, d_k) (如果为单头)。
        value: 值张量；形状为 (batch_size, num_heads, seq_len_v, d_v)
               或 (batch_size, seq_len_v, d_v) (如果为单头)。
               注意：seq_len_k 和 seq_len_v 必须相同。
        mask: 可选的掩码张量；形状应可广播到
              (batch_size, num_heads, seq_len_q, seq_len_k)。
              `True` 或 `1` 的位置将被掩盖（设为 -inf）。

    返回：
        包含以下内容的元组：
        - output：注意力输出张量；
                  形状为 (batch_size, num_heads, seq_len_q, d_v)
                  或 (batch_size, seq_len_q, d_v) (如果为单头)。
        - attention_weights：注意力权重张量；
                             形状为 (batch_size, num_heads, seq_len_q, seq_len_k)
                             或 (batch_size, seq_len_q, seq_len_k) (如果为单头)。
    """

    # 获取键向量的最后一维大小（d_k）
    d_k = query.size(-1)
    # 计算缩放点积：(Q @ K^T) / sqrt(d_k)
    scores = torch.matmul(query, key.transpose(-2, -1)) / math.sqrt(d_k)

    # 如果提供了掩码，则将掩码位置设为负无穷
    if mask is not None:
        scores = scores.masked_fill(mask, float('-inf'))

    # 对分数应用softmax以获得注意力权重，dim=-1表示最后一个维度，即seq_len_k，每一行
    attention_weights = torch.nn.functional.softmax(scores, dim=-1)

    # 处理NaN值，将其替换为0.0
    if torch.isnan(attention_weights).any():
        attention_weights = torch.nan_to_num(attention_weights, nan=0.0)

    # 使用注意力权重加权求和值向量
    output = torch.matmul(attention_weights, value)
    # 返回输出和注意力权重
    return output, attention_weights


class MultiHeadAttention(torch.nn.Module):
    # 定义多头注意力类，继承自torch.nn.Module
    def __init__(self, d_model: int, num_heads: int, dropout: float = 0.1):
        """
        初始化多头注意力模块。

        参数：
            d_model: 模型的隐藏维度。
            num_heads: 注意力头的数量。
            dropout: Dropout的概率。
        """

        # 调用父类的初始化方法
        super().__init__()

        # 断言d_model能被num_heads整除
        assert d_model % num_heads == 0, "d_model must be divisible by num_heads"

        # 存储模型隐藏维度
        self.d_model = d_model
        # 存储注意力头的数量
        self.num_heads = num_heads

        # 计算每个头的维度
        self.d_k = d_model // num_heads

        # 创建查询的线性变换层
        self.W_q = torch.nn.Linear(d_model, d_model)
        # 创建键的线性变换层
        self.W_k = torch.nn.Linear(d_model, d_model)
        # 创建值的线性变换层
        self.W_v = torch.nn.Linear(d_model, d_model)

        # 创建输出的线性变换层
        self.W_o = torch.nn.Linear(d_model, d_model)
        # 创建Dropout层
        self.dropout = torch.nn.Dropout(dropout)  # Dropout层（可选）

    def split_heads(self, x: torch.Tensor) -> torch.Tensor:
        """
        将输入张量拆分为多头。

        参数：
            x: 输入张量，形状为 (batch_size, seq_len, d_model)

        返回：
            拆分后的张量，形状为 (batch_size, num_heads, seq_len, d_k)
        """
        # 获取批量大小和序列长度
        batch_size, seq_len, _ = x.size()
        # 重新调整张量形状为 (batch_size, seq_len, num_heads, d_k)
        x = x.view(batch_size, seq_len, self.num_heads, self.d_k)
        # 转置第1和2维，得到 (batch_size, num_heads, seq_len, d_k)
        return x.transpose(1, 2)  # (batch_size, num_heads, seq_len, d_k)

    def combine_heads(self, x: torch.Tensor) -> torch.Tensor:
        """
        将多头张量合并为单头。

        参数：
            x: 多头张量，形状为 (batch_size, num_heads, seq_len, d_k)

        返回：
            合并后的张量，形状为 (batch_size, seq_len, d_model)
        """
        # 获取多头张量的维度信息
        batch_size, num_heads, seq_len, d_k = x.size()
        # 转置第1和2维得到 (batch_size, seq_len, num_heads, d_k)，并保证内存连续
        x = x.transpose(1, 2).contiguous()  # (batch_size, seq_len, num_heads, d_k)
        # 重新调整张量形状为 (batch_size, seq_len, d_model)
        return x.view(batch_size, seq_len, num_heads * d_k)  # (batch_size, seq_len, d_model)

    def forward(self, query: torch.Tensor, key: torch.Tensor, value: torch.Tensor, mask: torch.Tensor = None) -> torch.Tensor:
        """
        多头注意力的前向传播。

        参数：
            query: 查询张量，形状为 (batch_size, seq_len_q, d_model)
            key: 键张量，形状为 (batch_size, seq_len_k, d_model)
            value: 值张量，形状为 (batch_size, seq_len_k, d_model)
            mask: 可选的掩码张量，形状为 (batch_size, 1, seq_len_k) 或 (batch_size, seq_len_q, seq_len_k)

        返回：
            输出张量，形状为 (batch_size, seq_len_q, d_model)
            注意力权重，形状为 (batch_size, num_heads, seq_len_q, seq_len_k)
        """
        # 对查询进行线性变换
        Q = self.W_q(query)
        # 对键进行线性变换
        K = self.W_k(key)
        # 对值进行线性变换
        V = self.W_v(value)

        # 将查询拆分为多个头
        Q = self.split_heads(Q)
        # 将键拆分为多个头
        K = self.split_heads(K)
        # 将值拆分为多个头
        V = self.split_heads(V)

        # 计算缩放点积注意力并获得输出和权重
        output, attention_weights = scaled_dot_product_attention(Q, K, V, mask)

        # 将多个头的输出合并为单头
        output = self.combine_heads(output)

        # 对合并后的输出进行线性变换
        output = self.W_o(output)
        # 应用Dropout正则化
        output = self.dropout(output)

        # 返回最终输出和注意力权重
        return output, attention_weights

class PositionwiseFeedForward(torch.nn.Module):
    def __init__(self, d_model: int, d_ff: int, dropout: float = 0.1):
        super(PositionwiseFeedForward, self).__init__()
        self.linear1 = torch.nn.Linear(d_model, d_ff)
        self.activation = torch.nn.ReLU()
        self.linear2 = torch.nn.Linear(d_ff, d_model)
        self.dropout = torch.nn.Dropout(dropout)

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        x = self.linear1(x)
        x = self.activation(x)
        x = self.linear2(x)
        x = self.dropout(x)
        return x

class EncoderLayer(torch.nn.Module):
    def __init__(self, d_model: int, num_heads: int, d_ff: int, dropout: float = 0.1):
        # 调用父类初始化
        super(EncoderLayer, self).__init__()
        # 创建多头自注意力层
        self.self_attn = MultiHeadAttention(d_model, num_heads, dropout)
        # 创建前馈神经网络层
        self.feed_forward = PositionwiseFeedForward(d_model, d_ff, dropout)
        # 创建第一个层归一化
        self.norm1 = torch.nn.LayerNorm(d_model)
        # 创建第二个层归一化
        self.norm2 = torch.nn.LayerNorm(d_model)
        # 创建Dropout层
        self.dropout = torch.nn.Dropout(dropout)

    def forward(self, x: torch.Tensor, mask: torch.Tensor = None) -> torch.Tensor:
        # 自注意力子层
        attn_output, _ = self.self_attn(x, x, x, mask)  # 计算自注意力，获得注意力输出
        x = x + self.dropout(attn_output)  # 残差连接并应用Dropout
        x = self.norm1(x)  # 应用第一层LayerNorm

        # 前馈神经网络子层
        ff_output = self.feed_forward(x)  # 通过前馈神经网络处理输入
        x = x + self.dropout(ff_output)  # 残差连接并应用Dropout
        x = self.norm2(x)  # 应用第二层LayerNorm

        return x

class DecoderLayer(torch.nn.Module):
    def __init__(self, d_model: int, num_heads: int, d_ff: int, dropout: float = 0.1):
        # 调用父类初始化
        super(DecoderLayer, self).__init__()
        # 创建掩码多头自注意力层
        self.self_attn = MultiHeadAttention(d_model, num_heads, dropout)
        # 创建编码器-解码器注意力层
        self.enc_dec_attn = MultiHeadAttention(d_model, num_heads, dropout)
        # 创建前馈神经网络层
        self.feed_forward = PositionwiseFeedForward(d_model, d_ff, dropout)
        # 创建第一个层归一化
        self.norm1 = torch.nn.LayerNorm(d_model)
        # 创建第二个层归一化
        self.norm2 = torch.nn.LayerNorm(d_model)
        # 创建第三个层归一化
        self.norm3 = torch.nn.LayerNorm(d_model)
        # 创建Dropout层
        self.dropout = torch.nn.Dropout(dropout)

    def forward(self, x: torch.Tensor, enc_output: torch.Tensor, src_mask: torch.Tensor = None, tgt_mask: torch.Tensor = None) -> torch.Tensor:
        # 掩码自注意力子层
        attn_output, _ = self.self_attn(x, x, x, tgt_mask)  # 计算掩码自注意力，获得注意力输出
        x = x + self.dropout(attn_output)  # 残差连接并应用Dropout
        x = self.norm1(x)  # 应用第一层LayerNorm

        # 编码器-解码器注意力子层
        attn_output, _ = self.enc_dec_attn(x, enc_output, enc_output, src_mask)  # 计算编码器-解码器注意力，获得注意力输出
        x = x + self.dropout(attn_output)  # 残差连接并应用Dropout
        x = self.norm2(x)  # 应用第二层LayerNorm

        # 前馈神经网络子层
        ff_output = self.feed_forward(x)  # 通过前馈神经网络处理输入
        x = x + self.dropout(ff_output)  # 残差连接并应用Dropout
        x = self.norm3(x)  # 应用第三层LayerNorm

        return x
```